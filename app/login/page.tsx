/**
 * Login page — stub for Phase 0 build check.
 * Full implementation: Phase 1 (Auth).
 */
export default function LoginPage() {
  return (
    <main className="min-h-screen flex items-center justify-center" style={{ background: 'var(--bg-deepest)' }}>
      <div className="text-center">
        <div className="font-pixel text-xs mb-4" style={{ color: 'var(--text-gold)' }}>IRLXP</div>
        <h1 className="text-2xl font-bold mb-2" style={{ color: 'var(--text-primary)' }}>Continue Your Adventure</h1>
        <p style={{ color: 'var(--text-secondary)' }}>Authentication coming in Phase 1.</p>
      </div>
    </main>
  )
}
