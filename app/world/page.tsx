/**
 * World screen — stub for Phase 0 build check.
 * Full implementation: Phase 5 (World UI).
 */
export default function WorldPage() {
  return (
    <main className="min-h-screen flex items-center justify-center" style={{ background: 'var(--bg-deepest)' }}>
      <div className="text-center">
        <div className="font-pixel text-xs mb-4 text-gold">🌎 YOUR WORLD</div>
        <p style={{ color: 'var(--text-secondary)' }}>World screen coming in Phase 5.</p>
      </div>
    </main>
  )
}
