export default function CharacterPage() {
  return (
    <main className="min-h-screen flex items-center justify-center" style={{ background: 'var(--bg-deepest)' }}>
      <p style={{ color: 'var(--text-secondary)' }}>🧍 Character — Phase 7.</p>
    </main>
  )
}
